resources:
  queries:
    predicciones_query:
      name: "Query_Validacion_Predicciones"
      query: "SELECT * FROM ${var.catalog_name}.default.predicciones WHERE price < 0"

  alerts:
    alerta_precios_negativos:
      name: "Alerta Precios Erroneos"
      query_id: ${resources.queries.predicciones_query.id}
      parent_path: "/Shared/alerts"